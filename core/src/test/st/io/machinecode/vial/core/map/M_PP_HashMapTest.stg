delimiters "$", "$"

PPHashMapTest(I, P, p, hc, X) ::= <<
package io.machinecode.vial.core.map;

import io.machinecode.vial.api.Spread;
import junit.framework.Test;
import junit.framework.TestSuite;

import java.util.Map;

/**
 * @author <a href="mailto:brent.n.douglas@gmail.com">Brent Douglas</a>
 * @since 1.0
 */
public class $I$$I$HashMapTest {

    public static Test suite() {
        final TestSuite suite = new TestSuite($I$$I$HashMap.class.getName());
        $I$$I$HashMapSuite.createSuite(suite, "NONE", new $I$$I$HashMapSuite.CreateMap() {
            @Override
            public Map<$P$,$P$> create() {
                return new $I$$I$HashMap(4, 0.75f, Spread.NONE);
            }
        });
        $I$$I$HashMapSuite.createSuite(suite, "QUICK", new $I$$I$HashMapSuite.CreateMap() {
            @Override
            public Map<$P$,$P$> create() {
                return new $I$$I$HashMap(4, 0.75f, Spread.QUICK);
            }
        });
        $I$$I$HashMapSuite.createSuite(suite, "MURMUR3", new $I$$I$HashMapSuite.CreateMap() {
            @Override
            public Map<$P$,$P$> create() {
                return new $I$$I$HashMap(4, 0.75f, Spread.MURMUR3);
            }
        });
        return suite;
    }
}
>>