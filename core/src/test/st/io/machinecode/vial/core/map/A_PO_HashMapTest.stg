delimiters "$", "$"

POHashMapTest(I, P, p, hc, X) ::= <<
package io.machinecode.vial.core.map;

import io.machinecode.vial.api.Spread;
import junit.framework.Test;
import junit.framework.TestSuite;

import java.util.Map;

/**
 * @author <a href="mailto:brent.n.douglas@gmail.com">Brent Douglas</a>
 * @since 1.0
 */
public class $I$OHashMapTest {

    public static Test suite() {
        final TestSuite suite = new TestSuite($I$OHashMap.class.getName());
        $I$OHashMapSuite.createSuite(suite, "NONE", new $I$OHashMapSuite.CreateMap() {
            @Override
            public <V> Map<$P$,V> create() {
                return new $I$OHashMap<>(4, 0.75f, Spread.NONE);
            }
        });
        $I$OHashMapSuite.createSuite(suite, "QUICK", new $I$OHashMapSuite.CreateMap() {
            @Override
            public <V> Map<$P$,V> create() {
                return new $I$OHashMap<>(4, 0.75f, Spread.QUICK);
            }
        });
        $I$OHashMapSuite.createSuite(suite, "MURMUR3", new $I$OHashMapSuite.CreateMap() {
            @Override
            public <V> Map<$P$,V> create() {
                return new $I$OHashMap<>(4, 0.75f, Spread.MURMUR3);
            }
        });
        return suite;
    }
}

>>