delimiters "$", "$"

PSet(I, P, p, hck, hcv, X) ::= <<
package io.machinecode.vial.api.set;

import io.machinecode.vial.api.$I$Collection;

import java.util.Set;

/**
 * @author <a href="mailto:brent.n.douglas@gmail.com">Brent Douglas</a>
 * @since 1.0
 */
public interface $I$Set extends $I$Collection, Set<$P$> {

    $I$Set with(final $p$ value);

    $I$Set capacity(final int desired);
}
>>
