delimiters "$", "$"

PZCursor(I, P, p, hc, X) ::= <<
package io.machinecode.vial.api.map;

import java.util.Iterator;

/**
 * @author <a href="mailto:brent.n.douglas@gmail.com">Brent Douglas</a>
 * @since 1.0
 */
public interface $I$ZCursor extends Iterable<$I$ZCursor>, Iterator<$I$ZCursor> {

    $p$ key();

    boolean value();

    void reset();
}
>>
