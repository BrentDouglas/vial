delimiters "$", "$"

POCursor(I, P, p, hck, hcv, X) ::= <<
package io.machinecode.vial.api.map;

import io.machinecode.vial.api.OIterator;

/**
 * @author <a href="mailto:brent.n.douglas@gmail.com">Brent Douglas</a>
 * @since 1.0
 */
public interface $I$OCursor<V> extends Iterable<$I$OCursor<V$X$, OIterator<$I$OCursor<V$X$ {

    $p$ key();

    V value();
}
>>
